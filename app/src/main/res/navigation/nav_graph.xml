<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/home">

    <fragment android:id="@+id/home" android:name="com.example.themovieapp.home.HomeFragment"
        android:label="Home" tools:layout="@layout/home_fragment">
        <action android:id="@+id/action_homeFragment_to_movieListFragment" app:destination="@id/movie_search"/>
    </fragment>

    <fragment
        android:id="@+id/movie_search"
        android:name="com.example.themovieapp.search.MovieListFragment"
        android:label="GDG Search"/>

</navigation>